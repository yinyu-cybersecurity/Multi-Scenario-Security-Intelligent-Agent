# RAG Annotation:
# name: Exposed File Upload Form
# description: Exposed File Upload Form
# vuln_type: Sensitive Data Exposure
# severity: info
# cve_id: N/A


id: exposed-file-upload-form

info:
  name: Exposed File Upload Form
  author: geeknik
  severity: info
  classification:
    cpe: cpe:2.3:a:creativedream_file_uploader_project:creativedream_file_uploader:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: creativedream_file_uploader_project
    product: creativedream_file_uploader
    shodan-query: http.html:"multipart/form-data" html:"file"
  tags: miscellaneous,exposure,upload,form,misc,generic,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: regex
        regex:
          - <form.*?method=("|')?post("|')?.*?>
          - <form.*?(?i)(?-i)enctype=("|')?multipart\/form-data("|')?.*?>
          - <input.*?type=("|')?file("|')?.*?>
        condition: or

      - type: regex
        regex:
          - "type=[\"'](file)[\"']"
          - 'id="file"'
        condition: or