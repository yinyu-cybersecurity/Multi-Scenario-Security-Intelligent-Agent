# RAG Annotation:
# name: Add DOM EventListener - Detection
# description: Identifies the use of JavaScript addEventListener calls in the DOM.

# vuln_type: Cross-Site Scripting (XSS)
# severity: info
# cve_id: N/A




id: addeventlistener-detect

info:
  name: Add DOM EventListener - Detection
  author: yavolo,dwisiswant0
  severity: info
  description: |
    Identifies the use of JavaScript addEventListener calls in the DOM.
  reference:
    - https://portswigger.net/web-security/dom-based/controlling-the-web-message-source
  metadata:
    max-request: 1
  tags: miscellaneous,xss,misc,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: regex
        part: body
        regex:
          - (([\w\_]+)\.)?add[Ee]vent[Ll]istener\(["']?[\w\_]+["']? # Test cases: https://www.regextester.com/?fam=121118