# RAG Annotation:
# name: Mitel MiCollab Unified Communications Server (UCS) - Detect
# description: Mitel MiCollab UCS version disclosure via the /ucs/micollab/version.json endpoint.

# vuln_type: tech-detection
# severity: info
# cve_id: 




id: mitel-version-detect

info:
  name: Mitel MiCollab Unified Communications Server (UCS) - Detect
  author: aushack
  severity: info
  description: |
    Mitel MiCollab UCS version disclosure via the /ucs/micollab/version.json endpoint.
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"MiCollab End User Portal"
  tags: mitel,micollab,ucs,version,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/ucs/micollab/version.json"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "{\"version\":")'
          - 'contains(content_type, "text/plain")'
        condition: and

    extractors:
      - type: json
        part: body
        name: version
        group: 1
        json:
          - ".version"