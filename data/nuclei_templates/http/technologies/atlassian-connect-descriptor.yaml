# RAG Annotation:
# name: Atlassian Connect Descriptor - Detect
# description: The app descriptor is a JSON file ( atlassian-connect. json ) that describes the app to the Atlassian application. The descriptor includes general information for the app, as well as the modules that
# vuln_type: tech-detection
# severity: info
# cve_id: 


id: atlassian-connect-descriptor

info:
  name: Atlassian Connect Descriptor - Detect
  author: pussycat0x
  severity: info
  description: |
    The app descriptor is a JSON file ( atlassian-connect. json ) that describes the app to the Atlassian application. The descriptor includes general information for the app, as well as the modules that the app wants to use or extend.
  reference:
    - https://developer.atlassian.com/cloud/jira/platform/connect-app-descriptor/
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"atlassian-connect.json"
  tags: atlassian,tech,jira,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/atlassian-connect.json"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '"name":'
          - '"vendor":'
          - '"key":'
          - '"baseUrl":'
          - '"authentication":'
        condition: and

      - type: word
        part: header
        words:
          - application/json

      - type: status
        status:
          - 200