# RAG Annotation:
# name: Detect Microsoft SQL Server Reporting
# description: Detect Microsoft SQL Server Reporting
# vuln_type: tech-detection
# severity: info
# cve_id: 


id: sql-server-reporting

info:
  name: Detect Microsoft SQL Server Reporting
  author: puzzlepeaches
  severity: info
  metadata:
    max-request: 1
  tags: tech,microsoft,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/Reports/Pages/Folder.aspx"

    matchers:
      - type: word
        words:
          - "Report Manager"