# RAG Annotation:
# name: Default CodeIgniter Page
# description: Default CodeIgniter Page
# vuln_type: tech-detection
# severity: info
# cve_id: 


id: default-codeigniter-page

info:
  name: Default CodeIgniter Page
  author: dhiyaneshDK
  severity: info
  classification:
    cpe: cpe:2.3:a:codeigniter:codeigniter:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    vendor: codeigniter
    product: codeigniter
    shodan-query: http.title:"Welcome to CodeIgniter"
  tags: tech,codeigniter,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers-condition: and
    matchers:
      - type: regex
        regex:
          - '^<title>Welcome to CodeIgniter\s?(?:[0-9]!)?</title>$'

      - type: status
        status:
          - 200