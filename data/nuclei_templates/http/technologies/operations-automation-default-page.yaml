# RAG Annotation:
# name: Operations Automation Default Page
# description: Operations Automation Default Page
# vuln_type: tech-detection
# severity: info
# cve_id: 


id: operations-automation-default-page

info:
  name: Operations Automation Default Page
  author: dhiyaneshDK
  severity: info
  metadata:
    max-request: 1
    shodan-query: http.title:"Operations Automation Default Page"
  tags: tech,default,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>Operations Automation Default Page</title>'

      - type: status
        status:
          - 200