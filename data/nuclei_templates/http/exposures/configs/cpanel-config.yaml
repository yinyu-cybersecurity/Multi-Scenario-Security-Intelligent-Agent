# RAG Annotation:
# name: cPanel Configuration - File Disclosure
# description: cPanel configuration file is exposed and accessible, potentially leading to sensitive information disclosure.

# vuln_type: Sensitive Data Exposure
# severity: medium
# cve_id: N/A




id: cpanel-config

info:
  name: cPanel Configuration - File Disclosure
  author: DhiyaneshDk
  severity: medium
  description: |
    cPanel configuration file is exposed and accessible, potentially leading to sensitive information disclosure.
  metadata:
    verified: true
    max-request: 1
  tags: cpanel,config,exposure,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/cpanel.config"

    matchers:
      - type: dsl
        dsl:
          - "contains_all(body, 'configuration','user','access_log=')"
          - "status_code == 200"
        condition: and