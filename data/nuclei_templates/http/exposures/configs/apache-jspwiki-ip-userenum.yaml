# RAG Annotation:
# name: Apache JSPWiki - User IP Enumeration
# description: Enumerates the IP Address and users that is currently accessing an Apache JSPWiki web application, leading open source WikiWiki engine, feature-rich and built around standard JEE components (Java, ser
# vuln_type: Security Assessment
# severity: low
# cve_id: N/A


id: apache-jspwiki-ip-userenum

info:
  name: Apache JSPWiki - User IP Enumeration
  author: icarot
  severity: low
  description: |
    Enumerates the IP Address and users that is currently accessing an Apache JSPWiki web application, leading open source WikiWiki engine, feature-rich and built around standard JEE components (Java, servlets, JSP).
  classification:
    cpe: cpe:2.3:a:apache:jspwiki:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    verified: true
    vendor: apache
    product: jspwiki
  tags: jspwiki,apache,enumeration,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/Wiki.jsp?page=SystemInfo"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<b>Active Wiki Users</b>'
          - 'Number of active sessions'
        condition: and

      - type: status
        status:
          - 200