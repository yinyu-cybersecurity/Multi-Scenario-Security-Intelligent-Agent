# RAG Annotation:
# name: Rexify Configuration - Exposure
# description: Rexfile configuration from the Rex/Rexify automation framework was exposed. These files may contain SSH credentials, server hostnames, private key paths, and other sensitive data.

# vuln_type: Sensitive Data Exposure
# severity: high
# cve_id: N/A




id: rexify-config-exposure

info:
  name: Rexify Configuration - Exposure
  author: theamanrawat
  severity: high
  description: |
    Rexfile configuration from the Rex/Rexify automation framework was exposed. These files may contain SSH credentials, server hostnames, private key paths, and other sensitive data.
  reference:
    - https://www.rexify.org/
  metadata:
    verified: true
    max-request: 1
  tags: exposure,config,rexify,rex,devops

http:
  - method: GET
    path:
      - "{{BaseURL}}/Rexfile"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "use Rex"
          - "task"
          - "group"
          - "user"
          - "password"
          - "desc"
        condition: and
        part: body

      - type: status
        status:
          - 200