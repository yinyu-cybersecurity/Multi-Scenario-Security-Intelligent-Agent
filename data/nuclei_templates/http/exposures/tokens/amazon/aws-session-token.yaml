# RAG Annotation:
# name: AWS Session Token
# description: AWS Session Token
# vuln_type: Sensitive Data Exposure
# severity: info
# cve_id: N/A


id: aws-session-token

info:
  name: AWS Session Token
  author: DhiyaneshDK
  severity: info
  reference:
    - https://github.com/praetorian-inc/noseyparker/blob/main/crates/noseyparker/data/default/builtin/rules/aws.yml
  metadata:
    verified: true
    max-request: 1
  tags: aws,amazon,token,exposure,session,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        part: body
        regex:
          - '(?i)(?:aws.?session|aws.?session.?token|aws.?token)["''`]?\s{0,30}(?::|=>|=)\s{0,30}["''`]?([a-z0-9/+=]{16,200})[^a-z0-9/+=]'