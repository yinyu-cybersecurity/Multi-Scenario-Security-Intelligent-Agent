# RAG Annotation:
# name: Google OAuth Client Secret (prefixed)
# description: Google OAuth Client Secret (prefixed)
# vuln_type: Sensitive Data Exposure
# severity: info
# cve_id: N/A


id: google-oauth-prefixed

info:
  name: Google OAuth Client Secret (prefixed)
  author: DhiyaneshDK
  severity: info
  reference:
    - https://github.com/praetorian-inc/noseyparker/blob/main/crates/noseyparker/data/default/builtin/rules/google.yml
  metadata:
    verified: true
    max-request: 1
  tags: google,token,exposure,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        part: body
        regex:
          - '(GOCSPX-[a-zA-Z0-9_-]{28})'