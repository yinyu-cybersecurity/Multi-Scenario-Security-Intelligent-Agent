# RAG Annotation:
# name: SonarQube Token Disclosure
# description: SonarQube Token Disclosure
# vuln_type: Sensitive Data Exposure
# severity: info
# cve_id: N/A


id: sonarqube-token

info:
  name: SonarQube Token Disclosure
  author: Ice3man
  severity: info
  reference:
    - https://docs.sonarqube.org/latest/user-guide/user-token/
  metadata:
    max-request: 1
  tags: exposure,token,sonarqube,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    extractors:
      - type: regex
        part: body
        regex:
          - "sonar.{0,50}(?:\"|'|`)?[0-9a-f]{40}(?:\"|'|`)?"