# RAG Annotation:
# name: CodeSandbox phishing Detection
# description: A CodeSandbox phishing website was detected

# vuln_type: Security Assessment
# severity: info
# cve_id: N/A




id: codesandbox-phish

info:
  name: CodeSandbox phishing Detection
  author: rxerium
  severity: info
  description: |
    A CodeSandbox phishing website was detected
  reference:
    - https://codesandbox.io
  metadata:
    max-request: 1
  tags: phishing,codesandbox,developer,osint,discovery
http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>CodeSandbox: Instant Cloud Development Environments'

      - type: status
        status:
          - 200

      - type: dsl
        dsl:
          - '!contains(host,"codesandbox.io")'