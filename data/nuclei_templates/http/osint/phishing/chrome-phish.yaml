# RAG Annotation:
# name: chrome phishing Detection
# description: A chrome phishing website was detected

# vuln_type: Security Assessment
# severity: info
# cve_id: N/A




id: chrome-phish

info:
  name: chrome phishing Detection
  author: rxerium
  severity: info
  description: |
    A chrome phishing website was detected
  reference:
    - https://www.google.com/intl/en_uk/chrome/
  metadata:
    max-request: 1
  tags: phishing,chrome,osint,discovery
http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'Google Chrome – Download the fast, secure browser from Google'

      - type: status
        status:
          - 200

      - type: dsl
        dsl:
          - '!contains(host,"google.com")'