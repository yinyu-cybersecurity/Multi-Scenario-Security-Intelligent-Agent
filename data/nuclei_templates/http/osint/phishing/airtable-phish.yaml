# RAG Annotation:
# name: Airtable phishing Detection
# description: A Airtable phishing website was detected

# vuln_type: Security Assessment
# severity: info
# cve_id: N/A




id: airtable-phish

info:
  name: Airtable phishing Detection
  author: rxerium
  severity: info
  description: |
    A Airtable phishing website was detected
  reference:
    - https://airtable.com
  metadata:
    max-request: 1
  tags: phishing,airtable,productivity,osint,discovery
http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'Airtable: AI App Building for Enterprise - Airtable'

      - type: status
        status:
          - 200

      - type: dsl
        dsl:
          - '!contains(host,"airtable.com")'