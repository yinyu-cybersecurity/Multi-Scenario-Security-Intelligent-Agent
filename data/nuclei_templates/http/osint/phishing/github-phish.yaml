# RAG Annotation:
# name: github phishing Detection
# description: A github phishing website was detected

# vuln_type: Security Assessment
# severity: info
# cve_id: N/A




id: github-phish

info:
  name: github phishing Detection
  author: rxerium
  severity: info
  description: |
    A github phishing website was detected
  reference:
    - https://github.com
  metadata:
    max-request: 1
  tags: phishing,github,osint,discovery
http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'Sign in to GitHub · GitHub'

      - type: status
        status:
          - 200

      - type: dsl
        dsl:
          - '!contains(host,"github.com")'