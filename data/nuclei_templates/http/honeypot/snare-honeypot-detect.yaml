# RAG Annotation:
# name: Snare Honeypot - Detect
# description: Snare honeypot has been identified. The response to an incorrect HTTP version reveals a possible setup of the Snare web application honeypot.
# vuln_type: Honeypot Detection
# severity: info
# cve_id: N/A

id: snare-honeypot-detect

info:
  name: Snare Honeypot - Detect
  author: UnaPibaGeek
  severity: info
  description: |
    Snare honeypot has been identified.
    The response to an incorrect HTTP version reveals a possible setup of the Snare web application honeypot.
  metadata:
    verified: true
    max-request: 1
    vendor: snare
    product: http
    shodan-query:
      - '"Python/3.10 aiohttp/3.8.3" && Bad status'
      - '"python/3.10 aiohttp/3.8.3" && bad status'
  tags: snare,honeypot,ir,cti,discovery

http:
  - raw:
      - |
        GET / HTTP/1337
        Host: {{Hostname}}

    unsafe: true

    matchers-condition: and
    matchers:
      - type: word
        part: header
        words:
          - "Python/3.10 aiohttp/3.8.3"

      - type: word
        part: body
        words:
          - "Bad status line 'Expected dot'"