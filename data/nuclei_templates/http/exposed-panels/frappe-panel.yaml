# RAG Annotation:
# name: Frappe Panel - Detect
# description: Frappe ERPNext Login Panel was discovered.

# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A




id: frappe-panel

info:
  name: Frappe Panel - Detect
  author: Th3l0newolf
  severity: info
  description: |
    Frappe ERPNext Login Panel was discovered.
  reference:
    - https://frappeframework.com/
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"Login to Frappe"
  tags: frappe,login,web,erp,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/login#login"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<h4>Login to Frappe</h4>"
          - "frappe.csrf_token"
          - "frappe.boot"

      - type: status
        status:
          - 200