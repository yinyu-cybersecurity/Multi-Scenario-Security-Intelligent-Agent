# RAG Annotation:
# name: FortiClient Endpoint Management Server Panel - Detect
# description: FortiClient Endpoint Management Server Panel - Detect
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: forticlientems-panel

info:
  name: FortiClient Endpoint Management Server Panel - Detect
  author: h4sh5
  severity: info
  classification:
    cpe: cpe:2.3:a:fortinet:forticlient_endpoint_management_server:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: fortinet
    product: forticlient_endpoint_management_server
    shodan-query: http.favicon.hash:-800551065
  tags: panel,fortinet,forticlient,ems,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/signin"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "FortiClient Endpoint Management Server"

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        name: version
        group: 1
        regex:
          - 'VERSION_FULL\\u0022: \\u0022(.*?)\\u0022'