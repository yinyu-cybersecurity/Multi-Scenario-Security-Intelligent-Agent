# RAG Annotation:
# name: LockSelf Login Panel - Detect
# description: LockSelf login panel was detected.

# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A




id: lockself-panel

info:
  name: LockSelf Login Panel - Detect
  author: righettod
  severity: info
  description: |
    LockSelf login panel was detected.
  reference:
    - https://www.lockself.com/en/
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"LockSelf"
  tags: panel,lockself,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/application/index.html"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(body, "<title>LockSelf","content=\"LockSelf", "name=\"LockSelf")'
        condition: and