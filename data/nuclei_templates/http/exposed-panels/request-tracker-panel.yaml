# RAG Annotation:
# name: Request Tracker - Panel
# description: Request Tracker panel was discovered.

# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A




id: request-tracker-panel

info:
  name: Request Tracker - Panel
  author: burso
  severity: info
  description: |
    Request Tracker panel was discovered.
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.favicon.hash:203612613
  tags: panel,login,request,tracker,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'RT.Config'
          - 'RT.CurrentUser'
        condition: and

    extractors:
      - type: regex
        regex:
          - 'RT(?:\sVersion)?\s+(\d+\.\d+\.\d+)'
        group: 1