# RAG Annotation:
# name: Tableau Python Server Panel - Detect
# description: Tableau Python Server panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: tableau-panel

info:
  name: Tableau Python Server Panel - Detect
  author: pussycat0x
  severity: info
  description: Tableau Python Server panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    max-request: 1
    fofa-query: app="Tableau-Python-Server"
  tags: tableau,panel,python,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Tableau Python Server</title>'

      - type: status
        status:
          - 200