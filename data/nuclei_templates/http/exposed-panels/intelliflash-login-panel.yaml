# RAG Annotation:
# name: IntelliFlash Login Panel - Detect
# description: IntelliFlash login panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: intelliflash-login-panel

info:
  name: IntelliFlash Login Panel - Detect
  author: princechaddha
  severity: info
  description: IntelliFlash login panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    max-request: 1
  tags: panel,intelliflash,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>IntelliFlash</title>"

      - type: status
        status:
          - 200