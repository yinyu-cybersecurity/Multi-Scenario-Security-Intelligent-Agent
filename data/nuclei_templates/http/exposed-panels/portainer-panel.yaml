# RAG Annotation:
# name: Portainer Login Panel - Detect
# description: Portainer Login Panel - Detect
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: portainer-panel

info:
  name: Portainer Login Panel - Detect
  author: ritikchaddha
  severity: info
  reference:
    - https://github.com/portainer/portainer
  classification:
    cpe: cpe:2.3:a:portainer:portainer:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: portainer
    product: portainer
    shodan-query:
      - title:"Portainer"
      - http.title:"portainer"
    fofa-query: title="portainer"
    google-query: intitle:"portainer"
  tags: panel,portainer,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/#!/auth"

    stop-at-first-match: true
    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Portainer'
          - 'ng-app="portainer'
          - 'content="Portainer'
          - 'alt="Portainer'
          - 'portainer.auth'
        condition: or

      - type: status
        status:
          - 200