# RAG Annotation:
# name: Kopano WebApp Login Panel - Detect
# description: Kopano WebApp login panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: kopano-webapp-panel

info:
  name: Kopano WebApp Login Panel - Detect
  author: righettod
  severity: info
  description: Kopano WebApp login panel was detected.
  reference:
    - https://kopano.com/
  classification:
    cpe: cpe:2.3:a:kopano:webapp:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: kopano
    product: webapp
    shodan-query: http.title:"Kopano WebApp"
  tags: panel,kopano,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/webapp/"

    host-redirects: true
    max-redirects: 2
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(body, "<title>Kopano WebApp", "content=\"Kopano WebApp")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '\?kv([0-9.]+)"'