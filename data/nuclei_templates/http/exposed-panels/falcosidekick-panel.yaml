# RAG Annotation:
# name: Falcosidekick UI Login Panel - Detect
# description: Falcosidekick UI login panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: falcosidekick-panel

info:
  name: Falcosidekick UI Login Panel - Detect
  author: righettod
  severity: info
  description: Falcosidekick UI login panel was detected.
  reference:
    - https://github.com/falcosecurity/falcosidekick-ui
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.title:"Falcosidekick"
  tags: panel,falco,detect,login,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/login/'

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "falcosidekick ui", "falcosidekick-ui")'
        condition: and