# RAG Annotation:
# name: PHP LDAP Admin Panel - Detect
# description: PHP LDAP Admin Panel - Detect
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: phpldapadmin-panel

info:
  name: PHP LDAP Admin Panel - Detect
  author: ritikchaddha,DhiyaneshDk
  severity: info
  classification:
    cpe: cpe:2.3:a:phpldapadmin_project:phpldapadmin:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 3
    vendor: phpldapadmin_project
    product: phpldapadmin
    shodan-query: title:"phpLDAPadmin"
  tags: php,phpldapadmin,panel,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/phpldapadmin'
      - '{{BaseURL}}/htdocs/index.php'

    stop-at-first-match: true
    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "phpLDAPadmin ("
          - "/projects/phpldapadmin"
          - "alt=\"phpLDAPadmin logo"
        condition: or

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '<div id="ajFOOT">([0-9.]+)<\/div>'