# RAG Annotation:
# name: Pulse Secure Version
# description: Pulse Secure Version
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: pulse-secure-version

info:
  name: Pulse Secure Version
  author: dadevel
  severity: info
  classification:
    cpe: cpe:2.3:a:pulsesecure:pulse_connect_secure:*:*:*:*:*:*:*:*
  metadata:
    max-request: 2
    vendor: pulsesecure
    product: pulse_connect_secure
    shodan-query: cpe:"cpe:2.3:a:pulsesecure:pulse_connect_secure"
  tags: pulse,panel,pulsesecure,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/dana-na/nc/nc_gina_ver.txt" # not present in newer release
      - "{{BaseURL}}/dana-cached/hc/HostCheckerInstaller.osx"

    stop-at-first-match: true

    matchers-condition: or
    matchers:
      - type: word
        part: body
        words:
          - 'NAME="ProductVersion"'
          - "<key>version</key>"
        condition: or
        case-insensitive: true

    extractors:
      - type: regex
        group: 1
        part: body
        regex:
          - '(?i)NAME="ProductVersion"\s+?VALUE="([^"]+)"'

      - type: regex
        group: 1
        part: body
        regex:
          - "(?i)<string>([^<]+)</string>"