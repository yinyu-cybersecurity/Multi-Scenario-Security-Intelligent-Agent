# RAG Annotation:
# name: Xeams Admin Console Login Panel - Detect
# description: Xeams Admin Console login panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: xeams-admin-console

info:
  name: Xeams Admin Console Login Panel - Detect
  author: theamanrawat
  severity: info
  description: Xeams Admin Console login panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:synametrics:xeams:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: synametrics
    product: xeams
    shodan-query:
      - http.title:"Xeams Admin"
      - http.title:"xeams admin"
    fofa-query: title="xeams admin"
    google-query: intitle:"xeams admin"
  tags: panel,xeams,admin,console,synametrics,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/FrontController'

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Xeams Admin Console'
          - 'eXtended Email And Messaging Server</span>'
        condition: or

      - type: status
        status:
          - 200