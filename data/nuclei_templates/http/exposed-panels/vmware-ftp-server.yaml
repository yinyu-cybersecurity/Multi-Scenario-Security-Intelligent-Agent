# RAG Annotation:
# name: VMware FTP Server Login Panel - Detect
# description: VMware FTP Server login panel was detected.
# vuln_type: Exposed Panel/Detection
# severity: info
# cve_id: N/A


id: vmware-ftp-server

info:
  name: VMware FTP Server Login Panel - Detect
  author: DhiyaneshDK
  severity: info
  description: VMware FTP Server login panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: title:"VMWARE FTP SERVER"
  tags: panel,vmware,ftp,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/login'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'VMWARE FTP SERVER'

      - type: status
        status:
          - 200