# RAG Annotation:
# name: Mailchimp API Test
# description: Mailchimp API Test
# vuln_type: Security Assessment
# severity: info
# cve_id: N/A


id: api-mailchimp

info:
  name: Mailchimp API Test
  author: zzeitlin
  severity: info
  reference:
    - https://mailchimp.com/developer/transactional/docs/smtp-integration/#credentials-and-configuration
  metadata:
    max-request: 1
  tags: token-spray,mailchimp,tcp

self-contained: true
tcp:
  - inputs:
      - data: "AUTH PLAIN {{base64(hex_decode('00')+'apikey'+hex_decode('00')+token)}}\r\n"
        read: 1024
    host:
      - "tls://smtp.mandrillapp.com:465"
    matchers:
      - type: word
        words:
          - "success"