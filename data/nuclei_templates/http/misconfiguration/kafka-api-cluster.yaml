# RAG Annotation:
# name: Kafka Operation API - Cluster
# description: Kafka Operation API Cluster page was detected and appeared to be accessible without authentication.

# vuln_type: Security Misconfiguration
# severity: high
# cve_id: N/A




id: kafka-api-cluster

info:
  name: Kafka Operation API - Cluster
  author: DhiyaneshDk
  severity: high
  description: |
    Kafka Operation API Cluster page was detected and appeared to be accessible without authentication.
  classification:
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
  tags: kafka,misconfig,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/kafka/clusters"

    matchers:
      - type: dsl
        dsl:
          - 'contains_all(body, "bootstrap_servers", "brokers", "version")'
          - 'status_code == 200'
        condition: and