# RAG Annotation:
# name: PostgREST API Server  - Exposure
# description: PostgREST API Server was detected and appeared to be accessible without authentication.

# vuln_type: Authentication Bypass
# severity: high
# cve_id: N/A




id: postrest-api-exposure

info:
  name: PostgREST API Server  - Exposure
  author: DhiyaneshDk
  severity: high
  description: |
    PostgREST API Server was detected and appeared to be accessible without authentication.
  classification:
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"PostgREST"
  tags: postgrest,misconfig,unauth,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers:
      - type: dsl
        dsl:
          - 'contains_all(body, "PostgREST API","pgrst.object")'
          - 'status_code == 200'
        condition: and