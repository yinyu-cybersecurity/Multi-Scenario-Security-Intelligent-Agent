# RAG Annotation:
# name: Setup GitHub Enterprise - Detect
# description: Setup GitHub Enterprise - Detect
# vuln_type: Security Misconfiguration
# severity: unknown
# cve_id: N/A


id: setup-github-enterprise

info:
  name: Setup GitHub Enterprise - Detect
  author: tess
  severity: unknown
  classification:
    cpe: cpe:2.3:a:github:enterprise_server:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: github
    product: enterprise_server
    shodan-query: http.favicon.hash:-1373456171
  tags: misconfig,installer,github,setup,vuln

http:
  - method: GET
    path:
      - '{{BaseURL}}/setup/start'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Setup GitHub Enterprise"
          - "Install GitHub Enterprise"
        condition: and

      - type: status
        status:
          - 200