# RAG Annotation:
# name: Acunetix 360 Installer
# description: Acunetix 360 is susceptible to the Installation page exposure due to misconfiguration.
# vuln_type: Sensitive Data Exposure
# severity: info
# cve_id: N/A


id: acunetix-360-installer

info:
  name: Acunetix 360 Installer
  author: pussycat0x
  severity: info
  description: Acunetix 360 is susceptible to the Installation page exposure due to misconfiguration.
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.favicon.hash:-347188002
  tags: misconfig,exposure,install,acunetix,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/wizard/database/"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Acunetix 360 - Installation Wizard"
          - "License"
        condition: and

      - type: word
        part: header
        words:
          - "text/html"

      - type: status
        status:
          - 200