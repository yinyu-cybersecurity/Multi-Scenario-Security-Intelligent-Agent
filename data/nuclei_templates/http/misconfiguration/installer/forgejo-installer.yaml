# RAG Annotation:
# name: Forgejo Installation Page - Exposure
# description: Checks for the presence of a Forgejo Installer Page.

# vuln_type: Security Misconfiguration
# severity: high
# cve_id: N/A




id: forgejo-installer

info:
  name: Forgejo Installation Page - Exposure
  author: DhiyaneshDk
  severity: high
  description: |
    Checks for the presence of a Forgejo Installer Page.
  metadata:
    verified: true
    max-request: 1
    fofa-query: body="Installation - Forgejo:"
  tags: install,Forgejo,misconfig,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Installation - Forgejo: Beyond coding. We Forge.</title>"

      - type: status
        status:
          - 200