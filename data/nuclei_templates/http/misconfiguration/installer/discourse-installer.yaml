# RAG Annotation:
# name: Discourse Installer Exposure
# description: Discourse is susceptible to the Installation page exposure due to misconfiguration.
# vuln_type: Security Misconfiguration
# severity: high
# cve_id: N/A


id: discourse-installer

info:
  name: Discourse Installer Exposure
  author: DhiyaneshDk
  severity: high
  description: Discourse is susceptible to the Installation page exposure due to misconfiguration.
  classification:
    cpe: cpe:2.3:a:discourse:discourse:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: discourse
    product: discourse
    shodan-query: title:"Discourse Setup"
  tags: misconfig,discourse,install,vuln

http:
  - method: GET
    path:
      - '{{BaseURL}}/finish-installation/register'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'Discourse Setup'
          - 'Register Admin Account'
        condition: and

      - type: word
        part: header
        words:
          - "text/html"

      - type: status
        status:
          - 200