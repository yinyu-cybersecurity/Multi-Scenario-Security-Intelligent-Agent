# RAG Annotation:
# name: Prometheus Security Checks Workflow
# description: 运行所有Prometheus相关nuclei模板的安全检查工作流，检测Prometheus监控系统的安全漏洞
# vuln_type: workflow
# severity: medium
# cve_id: N/A

id: prometheus-workflow

info:
  name: Prometheus Security Checks
  author: daffainfo
  description: A simple workflow that runs all Prometheus related nuclei templates on a given target.
workflows:
  - template: http/exposed-panels/prometheus-exposed-panel.yaml
    subtemplates:
      - tags: prometheus
