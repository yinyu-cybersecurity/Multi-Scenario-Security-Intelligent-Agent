# RAG Annotation:
# name: SquirrelMail Security Checks Workflow
# description: 运行所有SquirrelMail相关nuclei模板的安全检查工作流，检测SquirrelMail Webmail的安全漏洞
# vuln_type: workflow
# severity: medium
# cve_id: N/A

id: squirrelmail-workflow

info:
  name: SquirrelMail Security Checks
  author: daffainfo
  description: A simple workflow that runs all SquirrelMail related nuclei templates on a given target.
workflows:
  - template: http/exposed-panels/squirrelmail-login.yaml
    subtemplates:
      - tags: squirrelmail
