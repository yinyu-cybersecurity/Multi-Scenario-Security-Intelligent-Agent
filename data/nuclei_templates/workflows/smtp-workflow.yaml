# RAG Annotation:
# name: SMTP Security Checks Workflow
# description: 运行所有SMTP相关nuclei模板的安全检查工作流，检测SMTP邮件服务的安全漏洞和配置问题
# vuln_type: workflow
# severity: medium
# cve_id: N/A

id: smtp-workflow

info:
  name: SMTP Security Checks
  author: pussycat0x
  description: A simple workflow that runs all smtp related nuclei templates on a given target.
workflows:
  - template: network/detection/smtp-detect.yaml
    subtemplates:
      - tags: smtp