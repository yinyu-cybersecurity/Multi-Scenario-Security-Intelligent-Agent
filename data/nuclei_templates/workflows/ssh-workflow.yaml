# RAG Annotation:
# name: SSH Security Checks Workflow
# description: 运行所有SSH相关nuclei模板的安全检查工作流，检测SSH服务的安全漏洞和配置问题
# vuln_type: workflow
# severity: medium
# cve_id: N/A

id: ssh-workflow

info:
  name: SSH Security Checks
  author: pussycat0x
  description: A simple workflow that runs all SSH related nuclei templates on a given target.
workflows:
  - template: network/detection/openssh-detect.yaml
    subtemplates:
      - tags: ssh