# RAG Annotation:
# name: GitList Security Checks Workflow
# description: 运行所有GitList相关nuclei模板的安全检查工作流，检测GitList代码仓库查看工具的安全漏洞
# vuln_type: workflow
# severity: medium
# cve_id: N/A

id: gitlist-workflow

info:
  name: GitList Security Checks
  author: daffainfo
  description: A simple workflow that runs all GitList related nuclei templates on a given target.
workflows:
  - template: http/technologies/tech-detect.yaml
    matchers:
      - name: gitlist
        subtemplates:
          - tags: gitlist
