# CTF-Agent 2.0 Phase 2.2: Skill系统实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现Skill系统，提供领域知识包、工作流模板、工具推荐。

**Architecture:** SkillRegistry管理Skill加载/匹配/推荐，YAML格式定义领域知识。

**Tech Stack:** Python 3.10+, PyYAML, asyncio

**Design Doc:** `docs/superpowers/specs/2026-04-01-phase2-complete-design.md`

**Depends On:** Phase 2.1 (FoundationCapability)

---

## File Structure

```
app/
├── skills/
│   ├── __init__.py           # 模块导出
│   └── registry.py           # SkillRegistry实现
skills/                       # Skill定义文件（项目根目录）
├── web_exploitation.yaml
├── internal_network.yaml
├── ad_attack.yaml
└── cloud_attack.yaml
tests/
└── skills/
    ├── __init__.py
    └── test_registry.py
```

---

## Task 1: 创建目录结构和模块初始化

**Files:**
- Create: `app/skills/__init__.py`
- Create: `tests/skills/__init__.py`

- [ ] **Step 1: 创建skills模块目录**

```python
# app/skills/__init__.py
"""
Skill系统

提供领域知识包：
- web_exploitation: Web渗透技能
- internal_network: 内网渗透技能
- ad_attack: 域渗透技能
- cloud_attack: 云安全技能
"""

from .registry import (
    SkillRegistry,
    Skill,
    SkillKnowledge,
    get_skill_registry,
)

__all__ = [
    "SkillRegistry",
    "Skill",
    "SkillKnowledge",
    "get_skill_registry",
]
```

- [ ] **Step 2: 创建测试目录**

```python
# tests/skills/__init__.py
"""Skill系统测试"""
```

---

## Task 2: 实现Skill数据模型

**Files:**
- Create: `app/skills/registry.py` (Part 1: 数据模型)

- [ ] **Step 1: 定义Skill数据结构**

```python
# app/skills/registry.py
"""
Skill系统实现

借鉴Claude Code的Skill设计：
- 领域知识包，提供专业知识
- 工作流模板，指导Agent执行
- 工具偏好，优化工具选择
"""

import os
import yaml
from typing import Dict, Any, List, Optional
from dataclasses import dataclass, field
from pathlib import Path


@dataclass
class WorkflowStep:
    """工作流步骤"""
    description: str
    suggested_tools: List[str] = field(default_factory=list)
    expected_output: str = ""


@dataclass
class Workflow:
    """工作流定义"""
    name: str
    description: str = ""
    steps: List[WorkflowStep] = field(default_factory=list)


@dataclass
class ToolPreference:
    """工具偏好"""
    tool_name: str
    score: float  # 0.0 - 1.0，越高越优先
    reason: str = ""


@dataclass
class Example:
    """示例场景"""
    scenario: str
    solution: str
    tools_used: List[str] = field(default_factory=list)


@dataclass
class Skill:
    """Skill定义"""
    name: str
    description: str
    domain: str
    
    # 知识内容
    knowledge: str = ""
    
    # 工作流模板
    workflows: List[Workflow] = field(default_factory=list)
    
    # 工具偏好
    tool_preferences: List[ToolPreference] = field(default_factory=list)
    
    # 示例
    examples: List[Example] = field(default_factory=list)
    
    # 元数据
    version: str = "1.0"
    tags: List[str] = field(default_factory=list)
    
    @classmethod
    def from_yaml(cls, yaml_path: str) -> "Skill":
        """从YAML文件加载Skill"""
        with open(yaml_path, 'r', encoding='utf-8') as f:
            data = yaml.safe_load(f)
        
        return cls._parse_dict(data)
    
    @classmethod
    def _parse_dict(cls, data: Dict) -> "Skill":
        """解析字典为Skill对象"""
        workflows = []
        for wf_data in data.get("workflows", []):
            steps = []
            for step_data in wf_data.get("steps", []):
                steps.append(WorkflowStep(
                    description=step_data.get("description", ""),
                    suggested_tools=step_data.get("suggested_tools", []),
                    expected_output=step_data.get("expected_output", "")
                ))
            workflows.append(Workflow(
                name=wf_data.get("name", ""),
                description=wf_data.get("description", ""),
                steps=steps
            ))
        
        tool_prefs = []
        for pref_data in data.get("tool_preferences", {}).items():
            if isinstance(pref_data[1], dict):
                tool_prefs.append(ToolPreference(
                    tool_name=pref_data[0],
                    score=pref_data[1].get("score", 0.5),
                    reason=pref_data[1].get("reason", "")
                ))
            else:
                tool_prefs.append(ToolPreference(
                    tool_name=pref_data[0],
                    score=float(pref_data[1]),
                    reason=""
                ))
        
        examples = []
        for ex_data in data.get("examples", []):
            examples.append(Example(
                scenario=ex_data.get("scenario", ""),
                solution=ex_data.get("solution", ""),
                tools_used=ex_data.get("tools_used", [])
            ))
        
        return cls(
            name=data.get("name", ""),
            description=data.get("description", ""),
            domain=data.get("domain", ""),
            knowledge=data.get("knowledge", ""),
            workflows=workflows,
            tool_preferences=tool_prefs,
            examples=examples,
            version=data.get("version", "1.0"),
            tags=data.get("tags", [])
        )
```

---

## Task 3: 实现SkillRegistry

**Files:**
- Modify: `app/skills/registry.py` (Part 2: Registry实现)

- [ ] **Step 1: 实现SkillRegistry类**

```python
# 继续添加到 registry.py

class SkillRegistry:
    """
    Skill注册表
    
    管理Skill的加载、匹配、推荐
    """
    
    def __init__(self):
        self._skills: Dict[str, Skill] = {}
        self._domain_index: Dict[str, List[str]] = {}
        self._tag_index: Dict[str, List[str]] = {}
    
    def register(self, skill: Skill) -> None:
        """注册Skill"""
        self._skills[skill.name] = skill
        
        # 建立域索引
        if skill.domain not in self._domain_index:
            self._domain_index[skill.domain] = []
        self._domain_index[skill.domain].append(skill.name)
        
        # 建立标签索引
        for tag in skill.tags:
            if tag not in self._tag_index:
                self._tag_index[tag] = []
            self._tag_index[tag].append(skill.name)
    
    def load_from_directory(self, directory: str) -> int:
        """
        从目录加载所有Skill
        
        Args:
            directory: Skill YAML文件目录
        
        Returns:
            加载的Skill数量
        """
        count = 0
        dir_path = Path(directory)
        
        if not dir_path.exists():
            return 0
        
        for yaml_file in dir_path.glob("*.yaml"):
            try:
                skill = Skill.from_yaml(str(yaml_file))
                self.register(skill)
                count += 1
            except Exception as e:
                print(f"加载Skill失败 {yaml_file}: {e}")
        
        return count
    
    def get_skill(self, name: str) -> Optional[Skill]:
        """获取指定Skill"""
        return self._skills.get(name)
    
    def list_skills(self) -> List[str]:
        """列出所有Skill名称"""
        return list(self._skills.keys())
    
    def get_skills_by_domain(self, domain: str) -> List[Skill]:
        """按域获取Skills"""
        skill_names = self._domain_index.get(domain, [])
        return [self._skills[name] for name in skill_names]
    
    def get_skills_by_tag(self, tag: str) -> List[Skill]:
        """按标签获取Skills"""
        skill_names = self._tag_index.get(tag, [])
        return [self._skills[name] for name in skill_names]
    
    def find_matching_skills(
        self,
        context: Dict[str, Any]
    ) -> List[Skill]:
        """
        根据上下文匹配合适的Skills
        
        Args:
            context: 包含task, target, vulns等信息的上下文
        
        Returns:
            匹配的Skill列表，按相关性排序
        """
        matches = []
        task = context.get("task", "").lower()
        target = context.get("target", "")
        vulns = context.get("vulns", [])
        
        for skill in self._skills.values():
            score = self._calculate_relevance(skill, task, target, vulns)
            if score > 0:
                matches.append((skill, score))
        
        # 按相关性排序
        matches.sort(key=lambda x: x[1], reverse=True)
        return [skill for skill, _ in matches]
    
    def _calculate_relevance(
        self,
        skill: Skill,
        task: str,
        target: str,
        vulns: List
    ) -> float:
        """计算Skill与任务的相关性"""
        score = 0.0
        
        # 任务描述匹配
        if skill.domain.lower() in task:
            score += 0.5
        
        # 标签匹配
        for tag in skill.tags:
            if tag.lower() in task:
                score += 0.3
        
        # 知识内容匹配
        knowledge_lower = skill.knowledge.lower()
        task_words = task.split()
        for word in task_words:
            if len(word) > 3 and word in knowledge_lower:
                score += 0.1
        
        # 工作流匹配
        for workflow in skill.workflows:
            if any(word in workflow.name.lower() for word in task_words if len(word) > 3):
                score += 0.2
        
        return min(score, 1.0)
    
    def get_tool_recommendation(
        self,
        skill_name: str,
        task_type: str = ""
    ) -> List[ToolPreference]:
        """
        获取工具推荐
        
        Args:
            skill_name: Skill名称
            task_type: 任务类型（可选）
        
        Returns:
            工具偏好列表，按score排序
        """
        skill = self.get_skill(skill_name)
        if not skill:
            return []
        
        prefs = sorted(skill.tool_preferences, key=lambda x: x.score, reverse=True)
        return prefs


# 全局注册表实例
_registry: Optional[SkillRegistry] = None


def get_skill_registry() -> SkillRegistry:
    """获取全局Skill注册表"""
    global _registry
    if _registry is None:
        _registry = SkillRegistry()
    return _registry
```

---

## Task 4: 创建核心Skill YAML文件

**Files:**
- Create: `skills/web_exploitation.yaml`
- Create: `skills/internal_network.yaml`

- [ ] **Step 1: 创建Web渗透Skill**

```yaml
# skills/web_exploitation.yaml
name: web_exploitation
description: Web应用渗透技能，涵盖信息收集、漏洞发现、漏洞利用
domain: web

version: "1.0"
tags:
  - web
  - http
  - injection
  - xss
  - sqli

knowledge: |
  ## Web渗透方法论
  
  ### 1. 信息收集
  - 端口扫描：识别Web服务端口
  - 目录枚举：发现隐藏路径和文件
  - 技术栈识别：确定框架、CMS、版本
  - 敏感文件：robots.txt, .git, backup文件
  
  ### 2. 漏洞发现
  - SQL注入：输入点检测、payload构造
  - XSS：反射型、存储型、DOM型
  - 文件上传：绕过限制、WebShell
  - 文件包含：LFI/RFI
  - SSRF：内网访问、云元数据
  
  ### 3. 漏洞利用
  - SQL注入：sqlmap自动化、手工注入
  - 命令注入：RCE获取
  - 反序列化：PHP/Java/Python链
  
  ### 4. 后渗透
  - 提权：内核漏洞、配置错误
  - 持久化：后门、定时任务
  - 横向移动：内网探测

workflows:
  - name: sql_injection_workflow
    description: SQL注入利用流程
    steps:
      - description: 识别注入点
        suggested_tools: ["sqlmap", "manual"]
        expected_output: "注入点URL和参数"
      - description: 确定数据库类型
        suggested_tools: ["sqlmap"]
        expected_output: "数据库类型和版本"
      - description: 枚举数据库
        suggested_tools: ["sqlmap"]
        expected_output: "数据库名列表"
      - description: 获取敏感数据
        suggested_tools: ["sqlmap"]
        expected_output: "用户名、密码等数据"
  
  - name: directory_enumeration_workflow
    description: 目录枚举流程
    steps:
      - description: 扫描常见目录
        suggested_tools: ["ffuf", "dirsearch"]
        expected_output: "发现的路径列表"
      - description: 检查敏感文件
        suggested_tools: ["curl", "nuclei"]
        expected_output: "robots.txt, .git等"

tool_preferences:
  sqlmap:
    score: 0.95
    reason: "SQL注入自动化利用，支持多种数据库"
  nuclei:
    score: 0.9
    reason: "漏洞扫描模板丰富，CVE覆盖广"
  ffuf:
    score: 0.85
    reason: "高性能目录枚举"
  nmap:
    score: 0.8
    reason: "端口扫描和服务识别"
  httpx:
    score: 0.75
    reason: "HTTP服务探测"

examples:
  - scenario: 发现SQL注入点
    solution: 使用sqlmap自动化利用，获取数据库信息
    tools_used: ["sqlmap"]
  - scenario: 需要枚举目录
    solution: 使用ffuf配合常见字典进行枚举
    tools_used: ["ffuf"]
```

- [ ] **Step 2: 创建内网渗透Skill**

```yaml
# skills/internal_network.yaml
name: internal_network
description: 内网渗透技能，涵盖主机发现、凭据获取、横向移动
domain: internal

version: "1.0"
tags:
  - internal
  - smb
  - rdp
  - credential
  - lateral

knowledge: |
  ## 内网渗透方法论
  
  ### 1. 主机发现
  - 存活探测：ICMP、TCP、ARP
  - 端口扫描：服务识别
  - 域环境：域控制器、域用户
  
  ### 2. 凭据获取
  - 密码喷洒：常见弱密码
  - 凭据转储：LSASS、SAM
  - 票据攻击：Kerberos
  - 哈希传递：PtH、PtT
  
  ### 3. 横向移动
  - SMB：psexec, wmi
  - RDP：远程桌面
  - WinRM：远程管理
  - SSH：密钥利用
  
  ### 4. 权限维持
  - 黄金票据：域持久化
  - 骨架密钥：后门
  - DCSync：域同步

workflows:
  - name: network_discovery_workflow
    description: 内网主机发现流程
    steps:
      - description: 存活主机扫描
        suggested_tools: ["fscan", "nmap"]
        expected_output: "存活主机列表"
      - description: 端口服务识别
        suggested_tools: ["nmap", "fscan"]
        expected_output: "开放端口和服务"
      - description: 识别域环境
        suggested_tools: ["bloodhound", "ldapsearch"]
        expected_output: "域信息和用户列表"
  
  - name: lateral_movement_workflow
    description: 横向移动流程
    steps:
      - description: 获取初始凭据
        suggested_tools: ["mimikatz", "crackmapexec"]
        expected_output: "用户名和密码/哈希"
      - description: 凭据验证
        suggested_tools: ["crackmapexec"]
        expected_output: "有效凭据列表"
      - description: 横向移动
        suggested_tools: ["crackmapexec", "impacket"]
        expected_output: "新主机访问权限"

tool_preferences:
  fscan:
    score: 0.95
    reason: "内网综合扫描工具，速度快"
  crackmapexec:
    score: 0.9
    reason: "内网渗透瑞士军刀"
  mimikatz:
    score: 0.85
    reason: "Windows凭据提取"
  impacket:
    score: 0.8
    reason: "Python内网工具集"
  bloodhound:
    score: 0.75
    reason: "域环境分析"

examples:
  - scenario: 内网主机发现
    solution: 使用fscan快速扫描内网段
    tools_used: ["fscan"]
  - scenario: 获取Windows凭据
    solution: 使用mimikatz提取LSASS内存
    tools_used: ["mimikatz"]
```

---

## Task 5: 编写单元测试

**Files:**
- Create: `tests/skills/test_registry.py`

- [ ] **Step 1: 创建测试文件**

```python
# tests/skills/test_registry.py
"""
SkillRegistry单元测试
"""

import os
import pytest
import tempfile
import yaml

from app.skills.registry import (
    SkillRegistry,
    Skill,
    Workflow,
    WorkflowStep,
    ToolPreference,
    Example,
    get_skill_registry,
)


class TestSkillModels:
    """测试Skill数据模型"""

    def test_workflow_step_creation(self):
        """测试工作流步骤创建"""
        step = WorkflowStep(
            description="扫描目标",
            suggested_tools=["nmap"],
            expected_output="端口列表"
        )
        assert step.description == "扫描目标"
        assert "nmap" in step.suggested_tools

    def test_skill_creation(self):
        """测试Skill创建"""
        skill = Skill(
            name="test_skill",
            description="测试技能",
            domain="test"
        )
        assert skill.name == "test_skill"
        assert skill.domain == "test"


class TestSkillFromYaml:
    """测试从YAML加载Skill"""

    def test_load_from_yaml(self, tmp_path):
        """测试YAML加载"""
        yaml_content = """
name: test_skill
description: 测试技能
domain: web
version: "1.0"
tags:
  - web
  - test
knowledge: |
  这是测试知识
workflows:
  - name: test_workflow
    steps:
      - description: 步骤1
        suggested_tools: ["tool1"]
tool_preferences:
  tool1: 0.9
examples:
  - scenario: 场景1
    solution: 解决方案1
"""
        yaml_file = tmp_path / "test_skill.yaml"
        yaml_file.write_text(yaml_content, encoding='utf-8')

        skill = Skill.from_yaml(str(yaml_file))

        assert skill.name == "test_skill"
        assert skill.domain == "web"
        assert "web" in skill.tags
        assert len(skill.workflows) == 1
        assert len(skill.tool_preferences) == 1
        assert skill.tool_preferences[0].score == 0.9


class TestSkillRegistry:
    """测试SkillRegistry"""

    def test_register_skill(self):
        """测试注册Skill"""
        registry = SkillRegistry()
        skill = Skill(name="test", description="测试", domain="test")

        registry.register(skill)

        assert "test" in registry.list_skills()
        assert registry.get_skill("test") == skill

    def test_load_from_directory(self, tmp_path):
        """测试从目录加载"""
        # 创建YAML文件
        yaml_content = """
name: skill1
description: 技能1
domain: web
tags: [web]
knowledge: 知识
"""
        yaml_file = tmp_path / "skill1.yaml"
        yaml_file.write_text(yaml_content, encoding='utf-8')

        registry = SkillRegistry()
        count = registry.load_from_directory(str(tmp_path))

        assert count == 1
        assert "skill1" in registry.list_skills()

    def test_find_matching_skills(self):
        """测试匹配Skills"""
        registry = SkillRegistry()

        # 注册测试Skills
        registry.register(Skill(
            name="web_skill",
            description="Web技能",
            domain="web",
            tags=["http", "injection"],
            knowledge="SQL注入 XSS漏洞"
        ))
        registry.register(Skill(
            name="internal_skill",
            description="内网技能",
            domain="internal",
            tags=["smb", "lateral"],
            knowledge="横向移动 域渗透"
        ))

        # 测试匹配
        matches = registry.find_matching_skills({
            "task": "发现SQL注入漏洞"
        })

        assert len(matches) > 0
        assert matches[0].name == "web_skill"

    def test_get_tool_recommendation(self):
        """测试工具推荐"""
        registry = SkillRegistry()

        skill = Skill(
            name="test",
            description="测试",
            domain="test",
            tool_preferences=[
                ToolPreference(tool_name="tool_a", score=0.9),
                ToolPreference(tool_name="tool_b", score=0.7),
            ]
        )
        registry.register(skill)

        recs = registry.get_tool_recommendation("test")

        assert len(recs) == 2
        assert recs[0].tool_name == "tool_a"
        assert recs[0].score > recs[1].score


class TestGlobalRegistry:
    """测试全局注册表"""

    def test_get_skill_registry(self):
        """测试获取全局注册表"""
        registry1 = get_skill_registry()
        registry2 = get_skill_registry()

        assert registry1 is registry2
```

---

## Task 6: 运行测试并验证

- [ ] **Step 1: 运行测试**

```bash
cd D:/LangGraph2.0/langGraph/deploy
pip install pyyaml -q
python -m pytest tests/skills/ -v
```

Expected: 所有测试通过

---

## Task 7: 提交并标记完成

- [ ] **Step 1: 提交代码**

```bash
git add app/skills/ skills/ tests/skills/
git commit -m "feat(skills): 实现Phase 2.2 Skill系统"
git tag -a v2.0-phase2.2 -m "Phase 2.2: Skill系统实现完成"
```

---

## Verification

```python
# 验证脚本
from app.skills import get_skill_registry

registry = get_skill_registry()
count = registry.load_from_directory("skills/")
print(f"加载了 {count} 个Skills")

# 匹配测试
matches = registry.find_matching_skills({"task": "SQL注入"})
print(f"匹配的Skills: {[s.name for s in matches]}")

# 工具推荐
for skill in matches:
    recs = registry.get_tool_recommendation(skill.name)
    print(f"{skill.name}推荐工具: {[r.tool_name for r in recs[:3]]}")
```

---

## Next Steps

Phase 2.2 完成后，继续：
- **Phase 2.3**: 工具迁移实现计划
- **Phase 2.4**: 自主Agent实现计划
- **Phase 2.5**: Coordinator集成实现计划
- **Phase 2.6**: 集成测试实现计划